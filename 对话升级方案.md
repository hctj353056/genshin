# HexAgent 对话能力升级方案

## 当前问题分析

### 1. 复读机根源
- **学习目标错误**：`OnlineLearner._learn_towards_input()` 的目标是"复制输入hex"
- 预训练目标是让输出 = 输入
- 学习算法只调整权重朝着输入方向走

### 2. 缺少对话上下文
- 每轮对话完全独立，没有历史记录
- 模型不知道"之前说了什么"

### 3. 学习算法简陋
- 使用随机噪声注入，非真正梯度下降
- 没有损失函数计算

---

## 升级方案

### Phase 1: 对话训练数据引擎

创建 `对话训练引擎.py`：
- 意图分类：问候、提问、闲聊、命令
- 上下文管理：保存对话历史
- 回复生成：从模板/检索到生成

### Phase 2: 改进HexMHA架构

```python
# 新增组件：
1. 对话上下文嵌入（历史对话的hex编码）
2. 意图识别头（识别用户意图类型）
3. 回复策略头（选择回复策略）
4. 语义相似度损失（不是复制，是相关）
```

### Phase 3: 新的学习算法

```python
# 从：学习目标=输入hex（复制）
# 改为：学习目标=语义相关的回复（生成）

损失函数设计：
1. 语义相似度损失：output与expected_reply的语义相似度
2. 意图匹配损失：output能表达正确的意图
3. 有效性损失：output必须是有效的UTF-8字符串
```

### Phase 4: 对话数据集

```python
# 对话对示例：
{
    "hello": "你好！有什么我可以帮助的吗？",
    "你好": "你好呀！今天过得怎么样？",
    "天气": "今天天气晴朗，适合出门！",
    "再见": "再见！有需要随时叫我~"
}
```

---

## 技术路线图

| 阶段 | 任务 | 复杂度 |
|------|------|--------|
| 1 | 对话数据集 + 意图模板 | ⭐ |
| 2 | 上下文窗口扩展 | ⭐⭐ |
| 3 | 改进学习目标 | ⭐⭐ |
| 4 | 语义损失函数 | ⭐⭐⭐ |
| 5 | 在线学习改进 | ⭐⭐ |
